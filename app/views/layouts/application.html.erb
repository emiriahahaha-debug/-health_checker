<!DOCTYPE html>
<html>
  <head>
    <link rel="icon" href="/icon.png" type="image/png">
    <title><%= content_for(:title) || "健康意識チェッカー" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="健康チェッカー">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA用マニフェスト（必要なら有効化） %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Tailwind CSSを正しく読み込む %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# JavaScript（Rails 7 + Importmaps / esbuild 前提） %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-[#f5f7f6] min-h-screen font-sans">
    <%# 全ページ共通の中央配置ラッパーは各ビューで使う %>
    <%= yield %>
  </body>
</html>
